# Claude — the AI-assisted overlay (.claude/ rules, skills, hooks, CLAUDE.md and
# docs/tickets/) is TRACKED in git; FerrisPad is an AI-assisted project. Only
# personal, machine-local overrides stay out (see also /CLAUDE.md and .mcp.json
# lower down).
.claude/settings.local.json

# macOS system files
.DS_Store

/target
/data
/docs/internal/*
/icons/*
# ...but track the generated app icons: the Flatpak build installs them
!/icons/hicolor

# Nix / direnv
.direnv/

# Flatpak build cache
.flatpak-builder/
/build-dir/

# Test artifacts
test_2gb.log
test_1_8gb.log
gen_log.exe
gen_log.pdb
scripts/generate_log.py
scripts/generate_log.rs

# Binaries
docs/assets/binaries/*

# Temporary build artifacts
FerrisPad.app/
FerrisPad.iconset/

# Sh excluded files
scripts/trigger_build_gh_worflow.sh
# Root-only CLAUDE.md (personal notes); the tracked instructions live in
# .claude/CLAUDE.md, which the leading slash here deliberately does NOT match.
/CLAUDE.md
.mcp.json
